Personal website built on hugo.

Make changes and run `hugo` to generate public assets before committing. After pushing changes, a github action will take care of deploying at https://loganthorneloe.com.

Domain managed by cloudflare.